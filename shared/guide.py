# -*- coding: utf-8 -*-
"""Small in-app guides for the main Larix Manager sections."""
from __future__ import annotations

import os

try:
    from PySide6 import QtWidgets, QtGui, QtCore  # type: ignore
except Exception:  # pragma: no cover - compatibility fallback
    from PyQt5 import QtWidgets, QtGui, QtCore  # type: ignore


_GUIDES = {
    "adapters": {
        "title": "Справка — Адаптеры",
        "intro": "Раздел создаёт и редактирует Adapter.xml: связывает общие атрибуты Larix с параметрами моделей и задаёт преобразования значений.",
        "steps": (
            "<b>«Выбор моделей»</b> — выбрать проект и модели, из которых нужно загрузить доступные параметры.",
            "<b>«Импорт XML»</b> — открыть существующий Adapter.xml и продолжить его редактирование.",
            "<b>«Импорт Excel»</b> — загрузить структуру адаптера из Excel. Можно выбрать несколько листов книги.",
            "<b>«Загрузить общие атрибуты»</b> — получить из Larix актуальный список общих атрибутов, для которых будут настраиваться привязки.",
            "<b>«Удалить выбранные»</b> — удалить отмеченные привязки у текущего атрибута.",
            "<b>«Преобразования»</b> — открыть настройки преобразования для выбранной привязки: замены и обработку исходного значения перед записью в атрибут.",
            "<b>«▲ / ▼»</b> — изменить приоритет привязок. Порядок важен, если у одного атрибута несколько источников.",
            "<b>«Сохранить адаптер»</b> — сохранить настроенный Adapter.xml.",
            "<b>«Сохранить общие атрибуты»</b> — выгрузить текущий список общих атрибутов в отдельный XML.",
            "В окнах выбора <b>«Выделить все / Снять все»</b> управляют всем списком, а <b>«Загрузить параметры»</b> загружает параметры отмеченных моделей.",
        ),
        "rules": (
            "Итоговый код формируется как «Группа параметров.Наименование параметра». Например BIM + Стадия проекта → BIM.Стадия проекта.",
            "«Список параметров» — возможные исходные параметры модели. Он не используется для сопоставления с LOI.",
            "Колонка «Параметры» — ключ автосопоставления LOI. Значение должно совпадать с названием LOI-заголовка; итоговый адаптированный параметр при этом может называться совершенно иначе.",
            "Если «Параметры» пусто, при импорте сопоставления используется «Наименование параметра» как запасной ключ. Надёжнее заполнять колонку явно.",
            "Если несколько строк имеют одинаковое значение в «Параметры», приложение предупредит о конфликте; при импорте будет использована последняя строка.",
            "При импорте Excel приложение также предупреждает о повторяющихся итоговых параметрах, несовпадающих типах и непарных значениях «Замена с / Замена на» вместо тихого объединения данных.",
            "«Тип параметра = Число» передаётся в проверку параметров как числовой тип.",
        ),
        "tips": (
            "Пример: LOI «Фаза» → «Параметры = Фаза» → BIM.Стадия проекта. Название итогового параметра не обязано совпадать с LOI.",
            "Кнопка «Из адаптера» в других разделах выбирает уже итоговый код адаптера; колонка «Параметры» в таком ручном выборе не участвует.",
        ),
    },
    "sets": {
        "title": "Справка — Наборы",
        "intro": "Раздел создаёт профиль наборов из Excel и при необходимости добавляет к элементам фильтры.",
        "steps": (
            "<b>«Выбрать»</b> в блоке «Источник данных» — выбрать Excel-книгу и один или несколько листов, из которых будут созданы наборы.",
            "Для каждой колонки Excel поле Larix можно ввести вручную или выбрать кнопками <b>«Из API»</b> и <b>«Из адаптера»</b>.",
            "<b>«Из API»</b> — выбрать существующий параметр непосредственно из проекта Larix.",
            "<b>«Из адаптера»</b> — выбрать итоговый код параметра с листа «Адаптер» выбранной Excel-книги либо из отдельного файла.",
            "Галочка слева от строки параметра включает или исключает эту колонку из сопоставления.",
            "<b>«Автонумерация (01_ 01.01_)»</b> — автоматически добавить номера разделам и их строкам, сохраняя порядок Excel.",
            "<b>«Фильтр»</b> — создать условия отбора по сопоставленным колонкам Excel.",
            "<b>«Распределить по группам»</b> — не объединять одинаковые параметры из разных групп в один элемент.",
            "<b>«Объединить листы в один профиль»</b> — собрать выбранные листы в один профиль; без галочки каждый лист создаётся отдельным профилем.",
            "<b>«Сгенерировать профиль»</b> — сформировать итоговый файл профиля наборов и предложить место сохранения.",
        ),
        "rules": (
            "Префиксы «01_...», «01.01_...», «01.01.01_...» задают уровни вложенности. Родитель определяется по числовому префиксу.",
            "Несколько значений в одной ячейке фильтра можно разделять запятой, точкой с запятой, переносом строки, «|», «/» или «\\».",
            "Строка без значений фильтра, в названии которой есть слово «Раздел», используется как раздел структуры. При автонумерации раздел получает 01_, 02_ и т. д., а его строки — 01.01_, 01.02_ в том же порядке, что и в Excel.",
            "При выборе нескольких листов режим «Объединить» создаёт один профиль; без него каждый лист становится отдельным профилем.",
            "Перед генерацией приложение предупреждает о неодинаковых колонках на выбранных листах, пустых сопоставлениях и конфликтующих структурных кодах.",
        ),
        "tips": (
            "Если в выбранной книге есть лист «Адаптер», окно «Из адаптера» сразу использует эту же книгу; отдельный файл выбирать не нужно.",
            "«Из адаптера» подставляет итоговый код, например BIM.Стадия проекта. Колонка «Параметры» здесь не участвует — она нужна для автоматического сопоставления LOI в разделе «Параметры».",
        ),
    },
    "matrix": {
        "title": "Справка — Матрицы",
        "intro": "Раздел создаёт профиль проверок коллизий по Excel-матрице и связанному списку наборов.",
        "steps": (
            "<b>«Выбрать»</b> напротив «Excel-файл» — выбрать Excel-книгу. После выбора укажите один <b>«Лист матрицы»</b>.",
            "<b>«Лист матрицы»</b> — выбрать один лист, из которого будет сформирован профиль матрицы.",
            "<b>«Группировать по разделам»</b> — создавать папки по парам разделов. Раздел берётся автоматически из префикса имени набора до первого «_»: АР_Стены → АР, КР_Колонны → КР. Если выключить, все проверки собираются в одну общую матрицу без отдельных блоков «Пересечение» и «Дублирование». Для значения B/D одна пара наборов получает сразу две проверки в одной матрице.",
            "<b>«Включить фильтр»</b> — добавить к наборам условия отбора по параметру.",
            "<b>«Выбрать»</b> напротив «Excel-файл наборов» — необязательный старый сценарий: указать отдельную книгу с наборами. Если ничего не выбирать, используется общая книга с матрицами.",
            "Поле <b>«Лист наборов»</b> выбирает лист, где находятся «Имя набора» и значения фильтра.",
            "<b>«Из API»</b> — выбрать параметр фильтрации из Larix; <b>«Из адаптера»</b> — выбрать итоговый параметр с листа адаптера.",
            "Поля <b>«Допуск A / B / C»</b> задают расстояния для соответствующих букв в матрице. D обозначает проверку на дублирование и отдельного допуска не требует.",
            "<b>«Сгенерировать профиль»</b> — сформировать итоговый .cv-файл по выбранным листам.",
        ),
        "rules": (
            "Имена в строках и столбцах матрицы должны совпадать с «Имя набора» на выбранном листе наборов.",
            "Отдельные подписи групп АР / КР / ОВ в Excel не нужны. Для группировки приложение использует часть имени набора до первого «_»: АР_..., КР_..., ОВ_... и т. д.",
            "Числовые фильтры можно записывать прямо в Excel: &gt;50, &lt;50, &gt;=50 / =&gt;50, &lt;=50 / =&lt;50, а также ≥50 и ≤50. "
            "Запись в конце названия тоже распознаётся, например «ВИС_Трубы≥50мм». Обычные значения без знака остаются точным текстовым совпадением.",
            "Если набор из матрицы отсутствует на листе наборов, проверка создастся, но для этого набора не будет сформирован фильтр; приложение покажет предупреждение.",
            "A/B/C — пересечения с тремя настраиваемыми допусками; D — дублирование; «D/B» создаёт обе проверки.",
            "Пара A ↔ B рассматривается как одна и та же независимо от направления. Заполнять зеркальную половину матрицы не требуется. Если зеркальные ячейки заполнены по-разному, приложение покажет предупреждение.",
            "Используйте только A, B, C и D. Неизвестные обозначения теперь не обрабатываются молча: приложение покажет предупреждение и пропустит некорректное значение.",
            "За одну генерацию используется один лист матрицы и, при включённом фильтре, один лист наборов.",
            "Если наборы слева и сверху различаются по составу, порядку, кодам или содержат дубли, приложение покажет предупреждение. Прямоугольная матрица может быть обработана, но часть пар в ней объективно отсутствует.",
            "Группировка по разделам включена по умолчанию, чтобы старые профили сохраняли привычную структуру. Её можно отключить перед генерацией.",
        ),
        "tips": (
            "Параметр фильтра можно выбрать из адаптера. «Из адаптера» выбирает итоговый адаптированный код; LOI-связь из колонки «Параметры» здесь не используется.",
            "В скачиваемом шаблоне все примеры Матриц собраны на одном листе «Матрица»: там есть A/B/C/D и наборы с условиями >, <, =, ≥ и ≤. Лист «Примеры синтаксиса» содержит все допустимые варианты записи, включая >= / => и <= / =<.",
        ),
    },
    "parameters": {
        "title": "Справка — Параметры",
        "intro": "Раздел создаёт профили проверки заполнения параметров по LOI-таблицам Excel.",
        "steps": (
            "<b>«Выбрать»</b> в блоке Excel — выбрать книгу и один или несколько листов с требованиями к параметрам.",
            "В блоке <b>«Поля фильтрации»</b> галочками отмечаются колонки Excel, которые должны участвовать в отборе элементов.",
            "<b>«Автонумерация»</b> — автоматически добавить числовые префиксы разделам и строкам структуры.",
            "<b>«Фильтры»</b> — включить создание условий отбора по отмеченным полям.",
            "Поле <b>«Логика: ИЛИ / И»</b> задаёт связь между группами фильтра: «ИЛИ» — достаточно одного совпадения, «И» — должны выполняться все выбранные группы.",
            "<b>«Группировать фильтры»</b> — собирать значения одного FieldName в отдельные блоки условий.",
            "<b>«Объединить листы в один профиль»</b> — объединить выбранные листы; без галочки для каждого листа создаётся отдельный профиль.",
            "<b>«Открыть»</b> в блоке «Сопоставление параметров» — открыть окно, где LOI-заголовки связываются с параметрами Larix.",
            "В окне сопоставления <b>«Из API»</b> выбирает параметр из Larix, <b>«Из адаптера»</b> — итоговый код адаптера, а <b>«Импорт адаптера»</b> автоматически сопоставляет LOI по колонке «Параметры» листа адаптера.",
            "Там же <b>«Сохранить JSON»</b> сохраняет текущее сопоставление, <b>«Импорт JSON»</b> загружает сохранённое сопоставление, <b>«Очистить сопоставление»</b> сбрасывает его.",
            "<b>«Сгенерировать профиль»</b> — сформировать итоговый .pv-файл проверки параметров.",
        ),
        "rules": (
            "«Импорт адаптера» сопоставляет LOI по колонке «Параметры» листа адаптера. Например LOI «Фаза» + «Параметры = Фаза» → BIM.Стадия проекта.",
            "Название итогового параметра не обязано совпадать с LOI. «Список параметров» при таком сопоставлении не участвует.",
            "LOI без совпадения в адаптере остаётся доступным для ручного сопоставления. Если в колонке «Параметры» есть дубли, приложение покажет предупреждение; используется последняя строка.",
            "Если один и тот же LOI-заголовок встречается на нескольких выбранных листах книги, для него используется одно общее сопоставление.",
            "Классификационная структура: «01_Раздел» → «01.01_Подраздел» → «01.01.01_Группа». Родитель определяется по ближайшему существующему коду.",
            "Ручная структура: «# / ## / ###» или «1# / 2# / 3#». Если родитель уровня не найден, приложение предупреждает и использует ближайший доступный уровень.",
            "Обычная текстовая строка внутри классификационного раздела становится отдельным уровнем; последующий «# ...» вкладывается уже в эту строку.",
            "Структурный код в названии строки и столбец «Код по классификатору» независимы: первый строит дерево, второй задаёт значение фильтра.",
            "Несколько значений одного поля всегда остаются альтернативами внутри своей группы. Например «Стены, Перекрытия» означает «Стены ИЛИ Перекрытия», даже если общая логика фильтра выбрана «И».",
            "Если один структурный код повторно используется с другим названием или родителем, либо несколько колонок фильтра сопоставлены одному FieldName, приложение выводит предупреждение.",
        ),
        "tips": (
            "Автоматическое сопоставление через адаптер удобно, если одни и те же LOI используются во многих Excel-листах.",
        ),
    },
}


class GuideDialog(QtWidgets.QDialog):
    def __init__(self, topic: str, parent=None):
        super().__init__(parent)
        cfg = _GUIDES.get(topic) or _GUIDES["parameters"]
        self.setWindowTitle(cfg["title"])
        self.resize(720, 560)
        self.setModal(False)

        root = QtWidgets.QVBoxLayout(self)
        root.setContentsMargins(18, 18, 18, 18)
        root.setSpacing(12)

        title = QtWidgets.QLabel(cfg["title"])
        f = title.font()
        f.setPointSize(max(14, f.pointSize() + 4))
        f.setBold(True)
        title.setFont(f)
        root.addWidget(title)

        browser = QtWidgets.QTextBrowser()
        browser.setOpenExternalLinks(False)
        browser.setFrameShape(QtWidgets.QFrame.NoFrame)
        steps = "".join(f"<li>{s}</li>" for s in cfg.get("steps", ()))
        rules = "".join(f"<li>{s}</li>" for s in cfg.get("rules", ()))
        tips = "".join(f"<li>{s}</li>" for s in cfg.get("tips", ()))
        parts = [
            "<div style='font-size: 11pt; line-height: 1.45'>",
            f"<p>{cfg['intro']}</p>",
            "<div style='margin: 10px 0; padding: 9px 11px; border: 1px solid #8dbb94; border-radius: 6px;'>"
            "<b>Примеры Excel:</b> готовые примеры для Адаптеров, Наборов, Матриц и Параметров находятся в общем демонстрационном шаблоне. "
            "На главном экране нажмите <b>«Скачать шаблон»</b>."
            "</div>",
            "<h3>Кнопки и элементы управления</h3>",
            f"<ul>{steps}</ul>",
        ]
        if rules:
            parts.extend(("<h3>Правила и важные моменты</h3>", f"<ul>{rules}</ul>"))
        if tips:
            parts.extend(("<h3>Полезно знать</h3>", f"<ul>{tips}</ul>"))
        parts.append("</div>")
        browser.setHtml("".join(parts))
        root.addWidget(browser, 1)

        buttons = QtWidgets.QDialogButtonBox(QtWidgets.QDialogButtonBox.Close)
        close_btn = buttons.button(QtWidgets.QDialogButtonBox.Close)
        if close_btn is not None:
            close_btn.setText("Закрыть")
        buttons.rejected.connect(self.close)
        buttons.accepted.connect(self.close)
        root.addWidget(buttons)


def _find_info_icon(icon_dir: str) -> str:
    for name in ("circle-info.png", "circle-info.ico", "info.png"):
        p = os.path.join(icon_dir, name)
        if os.path.exists(p):
            return p
    return ""


def create_guide_button(parent, topic: str, *, icon_dir: str) -> QtWidgets.QPushButton:
    """Create the shared «Справка» button for a main section."""
    btn = QtWidgets.QPushButton("Справка", parent)
    btn.setObjectName("guideInfoButton")
    btn.setToolTip("Как пользоваться этим разделом")
    # Keep this as a regular application button so it inherits the same
    # border, background, hover and pressed states as the rest of the UI.
    btn.setMinimumHeight(34)
    btn.setMinimumWidth(108)
    btn.setFlat(False)
    btn.setCursor(QtGui.QCursor(QtCore.Qt.PointingHandCursor))
    icon_path = _find_info_icon(icon_dir)
    if icon_path:
        btn.setIcon(QtGui.QIcon(icon_path))
        btn.setIconSize(QtCore.QSize(18, 18))
    else:
        try:
            btn.setIcon(parent.style().standardIcon(QtWidgets.QStyle.SP_MessageBoxInformation))
            btn.setIconSize(QtCore.QSize(18, 18))
        except Exception:
            pass

    def _open():
        dlg = GuideDialog(topic, parent)
        # Keep a reference so a modeless dialog cannot be garbage-collected.
        setattr(parent, "_larix_guide_dialog", dlg)
        dlg.show()
        dlg.raise_()
        dlg.activateWindow()

    btn.clicked.connect(_open)
    return btn
